name: long_time_no_notification
description: A package for easy management of showing and hiding notifications once closed!
version: 0.0.2
repository: https://github.com/okaryo/long_time_no_notification

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.10
  flutter_lints: ^1.0.0
  mockito: ^5.1.0
